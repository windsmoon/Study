# 湍流函数（Turbulence）学习总结

## 1. 湍流函数

图形学中的湍流函数通常由多层噪声叠加得到：

$$
T(p)=\sum_{i=0}^{N-1} A_i |Noise(p \cdot F_i)|
$$

典型形式：

$$
T(p)=|Noise(p)|
+0.5|Noise(2p)|
+0.25|Noise(4p)|
+0.125|Noise(8p)|
+\cdots
$$

常用于：

* 云
* 烟雾
* 火焰
* 岩石
* 大理石纹理
* 流体扰动

---

## 2. Turbulence 和 fBm

普通 fBm：

$$
fBm(p)=Noise(p)+0.5Noise(2p)+0.25Noise(4p)+...
$$

Turbulence：

$$
T(p)=|Noise(p)|+0.5|Noise(2p)|+0.25|Noise(4p)|+...
$$

主要区别：

$$
\boxed{Turbulence \approx 取绝对值的 fBm}
$$

`abs()` 会把负值翻到正值，使噪声出现更明显的脊状、褶皱结构。

---

## 3. 倍频

湍流函数中：

$$
Noise(p),\ Noise(2p),\ Noise(4p),\ Noise(8p)
$$

就是不断提高频率：

$$
f,\ 2f,\ 4f,\ 8f
$$

每一层通常称为一个 **Octave（倍频层）**。

频率越高：

* 噪声变化越快
* 纹理越密
* 产生的细节尺度越小

所以：

* 低频负责大尺度形状
* 高频负责小尺度细节

与此同时通常降低振幅：

$$
1,\ 0.5,\ 0.25,\ 0.125
$$

避免高频细节盖过整体结构。

---

## 4. p 是什么

\(p\) 是噪声函数的**采样位置**。

二维：

$$
p=(x,y)
$$

三维：

$$
p=(x,y,z)
$$

例如：

```hlsl
float n = Noise(worldPos);
```

这里：

```text
p = worldPos
```

---

## 5. 为什么乘 2 会提高频率

例如：

$$
Noise(p)
$$

变为：

$$
Noise(2p)
$$

相当于把输入坐标：

$$
(x,y)\rightarrow(2x,2y)
$$

因此在相同空间距离内，会经过更多次噪声变化。

所以：

$$
\boxed{p=\text{采样位置}}
$$

$$
\boxed{2p=\text{以 2 倍空间频率采样}}
$$

湍流函数中的倍频，本质上就是不断放大噪声函数的输入坐标。
