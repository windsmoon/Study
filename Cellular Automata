# 细胞自动机学习总结

## 1. 核心概念
- 将世界划分成格子，每个格子有自己的状态。
- 每个格子根据周围邻居的状态，决定下一轮自己的状态。
- 本质：**简单的局部规则，通过多轮迭代形成复杂的整体结构。**

## 2. 洞穴生成中的基本状态
- `0`：空地
- `1`：墙

通常先随机初始化地图，例如按一定概率生成墙。

## 3. 邻域规则
常见使用 **8 邻域**：

```text
x x x
x O x
x x x
```

其中 `O` 是当前格子，其余 8 个都是邻居。

示例规则：

```text
邻居墙数量 >= 5 → 墙
邻居墙数量 < 5  → 空地
```

- 阈值越低：越容易变成墙，墙更多。
- 阈值越高：越难变成墙，空地更多。
- 迭代次数越多：地图通常越平滑，但过多会损失细节。

## 4. 同步更新 / 双缓冲
不能一边遍历一边直接修改当前地图，否则前面格子的新状态会影响后面格子的计算。

正确方式：

```text
CurrentMap：只读
NextMap：只写
```

一轮全部计算完成后，再交换两个数组。

核心原因：

> 避免同一轮中，后计算的格子受到前面已经修改的格子影响。

## 5. 边界处理
生成封闭洞穴时，通常把地图外部视为墙：

```text
邻居坐标越界 → 墙数量 +1
```

这样地图边缘更容易被封住。

## 6. Birth / Survival
可以让墙和空地使用不同规则：

```text
当前是墙：
邻居墙 >= SurvivalThreshold → 保持墙

当前是空地：
邻居墙 >= BirthThreshold → 变成墙
```

理解：

- `SurvivalThreshold`：控制已有墙有多容易保留下来。
- `BirthThreshold`：控制新墙有多容易产生和扩张。
- `BirthThreshold` 越低 → 墙扩张越快。

## 7. 连通区域处理
细胞自动机只能生成形状，**不能保证洞穴互相连通**。

因此生成后通常需要：

```text
Flood Fill / BFS / DFS
```

找出所有连通区域。

连通规则应和游戏移动规则一致：

- 只能上下左右移动 → **4 邻域**
- 可以斜向移动 → **8 邻域**

## 8. 删除小区域
找到所有区域后，可以删除太小的孤立区域。

例如：

```text
区域面积 < 50 → 删除
```

小空地区域通常填回墙，可以减少无意义的小洞穴。

## 9. 连接洞穴区域
如果剩下多个大区域，需要挖通道把它们连接起来。

基本流程：

```text
找区域边界
→ 比较区域之间最近的边界点
→ 在最近点之间挖通道
```

只比较边界点，比所有格子两两比较效率更高。

## 10. 保证所有区域连通
有 `N` 个彼此独立的区域，至少需要：

```text
N - 1
```

条通道才能全部连起来。

不能单纯一直连接“全局最近的两个区域”，否则可能重复连接已经连通的区域。

更合理的方法：

1. 选一个主区域。
2. 维护“已连通区域”和“未连通区域”。
3. 每次把距离最近的未连通区域接进来。
4. 重复直到全部连通。

这个思路类似 **Prim 最小生成树**。

## 11. 游戏洞穴生成完整流程

```text
随机初始化地图
        ↓
细胞自动机多轮平滑
        ↓
Flood Fill 找连通区域
        ↓
删除过小区域
        ↓
寻找区域边界
        ↓
连接主要区域
        ↓
生成最终可玩的洞穴地图
```

## 12. 最核心的几个知识点
- 细胞自动机是 **局部规则 → 全局结构**。
- 更新时应使用 **双缓冲 / 同步更新**。
- 洞穴生成常用 **8 邻域统计墙数量**。
- 后处理需要解决 **小区域和连通性**。
- Flood Fill 判断连通时，应与角色的实际移动规则一致。
- 多区域连接可以使用类似 **最小生成树** 的思路。
