# SH 球谐函数学习总结

## 1. SH 是什么

SH（Spherical Harmonics，球谐函数）可以理解为：

> 用一组固定的球面基函数，去表示一个定义在球面方向上的函数。

例如环境光：

$$
L(\mathbf d)
$$

表示：

> 给定方向 \(\mathbf d\)，返回这个方向来的光照。

所有单位方向：

$$
|\mathbf d|=1
$$

组成单位球面，因此 \(L(\mathbf d)\) 是一个球面函数。

---

## 2. SH 基函数

SH 用一组固定基函数表示球面函数：

$$
L(\mathbf d)
\approx
\sum_{l,m}c_l^mY_l^m(\mathbf d)
$$

其中：

* \(Y_l^m\)：SH 基函数
* \(c_l^m\)：对应 SH 系数

可以理解为：

> 基函数是固定积木，SH 系数表示每块积木使用多少。

---

## 3. \(l\) 和 \(m\)

### \(l\)

表示 SH 的阶 / 频率等级。

* \(l=0\)：常量、最低频
* \(l=1\)：一阶方向变化
* \(l=2\)：更复杂的二阶变化
* 阶越高，可以表达越细的方向变化

### \(m\)

表示同一个 \(l\) 中的不同基函数：

$$
m=-l,\ldots,0,\ldots,l
$$

因此每一阶有：

$$
2l+1
$$

个基函数。

例如：

* \(l=0\)：1 个
* \(l=1\)：3 个
* \(l=2\)：5 个

如果使用到 \(l=2\)，实际上包含：

$$
l=0,1,2
$$

因此总共：

$$
1+3+5=9
$$

个基函数。

一般使用到最高阶 \(L\) 时：

$$
\boxed{(L+1)^2}
$$

个基函数。

---

## 4. 常见 SH 基函数的直观形式

设单位方向：

$$
\mathbf d=(x,y,z)
$$

忽略归一化常数，可以近似理解为：

### \(l=0\)

$$
1
$$

表示整体平均值。

### \(l=1\)

$$
x,\quad y,\quad z
$$

表示三个方向上的一阶变化。

### \(l=2\)

类似：

$$
xy,\quad yz,\quad xz,\quad x^2-y^2,\quad 3z^2-1
$$

表示更复杂的二阶方向变化。

---

## 5. SH 基函数的正交

不同 SH 基函数之间彼此正交。

这里的“正交”不是空间中成 90°，而是：

$$
\int_{\Omega}Y_i(\mathbf d)Y_j(\mathbf d)d\omega=0
\quad (i\neq j)
$$

也就是：

> 两个球面函数的内积为 0。

因此不同 SH 基函数可以独立描述不同的方向模式，不会重复记录同一份信息。

---

## 6. 为什么多个 SH 系数相加不会把亮度提高

9 个 SH 系数不是 9 份完整光照。

它们是原始光照被拆出的 9 个独立分量：

* \(l=0\)：平均亮度
* \(l=1\)：一阶方向信息
* \(l=2\)：二阶方向信息

高阶基函数本身具有正负区域，整个球面上的平均贡献通常会互相抵消。

因此增加 SH 阶数主要增加的是：

> 对方向细节的表达能力。

而不是增加整体亮度。

---

## 7. 如何计算 SH 系数

原始球面函数：

$$
L(\mathbf d)
$$

投影到第 \(i\) 个 SH 基函数：

$$
c_i
=
\int_{\Omega}
L(\mathbf d)Y_i(\mathbf d)d\omega
$$

可以理解为：

> 计算原始光照与这个基函数的“函数点积”。

实际程序通常通过离散采样：

$$
c_i
\approx
\sum_k
L(\mathbf d_k)
Y_i(\mathbf d_k)
w_k
$$

其中：

* \(\mathbf d_k\)：采样方向
* \(L(\mathbf d_k)\)：这个方向的真实光照
* \(Y_i(\mathbf d_k)\)：该 SH 基函数在这个方向上的值
* \(w_k\)：该采样代表的球面面积 / 立体角

如果球面均匀采样：

$$
w_k=\frac{4\pi}{N}
$$

所有权重相同。

---

## 8. 如何从 SH 恢复颜色

有了 SH 系数以后，给定方向 \(\mathbf d\)：

$$
L(\mathbf d)
\approx
\sum_i c_iY_i(\mathbf d)
$$

如果光照是 RGB，则每个 \(c_i\) 本身就是 RGB：

$$
c_i=(R_i,G_i,B_i)
$$

而：

$$
Y_i(\mathbf d)
$$

只是一个标量权重。

因此：

$$
\text{Color}(\mathbf d)
=
\sum_i
c_iY_i(\mathbf d)
$$

即：

> 当前方向 → 计算所有 SH 基函数值 → 加权所有 RGB SH 系数 → 得到颜色。

---

## 9. SH 为什么适合低频光照

低阶 SH 只能表达比较平滑的球面变化。

因此：

* 平滑环境光：低阶 SH 很适合
* 很小、很亮的太阳：需要高阶 SH
* 尖锐镜面反射：低阶 SH 不适合

所以 SH 特别适合：

* 环境光
* 间接光
* Light Probe
* 漫反射

而 Reflection Probe / CubeMap 更适合：

* 镜面反射
* 高频环境细节

---

# Light Probe 与 SH

## 10. Light Probe 存什么

一个 Light Probe 表示：

> 这个空间位置周围，各个方向来的低频环境光。

通常使用二阶 SH：

$$
l=0,1,2
$$

共：

$$
9
$$

个 SH 基函数。

RGB 光照下就是：

$$
9\times3=27
$$

个浮点分量。

---

## 11. 法线与漫反射

漫反射并不是只看法线正前方的一束光。

对于法线 \(N\)，实际应该积分整个半球：

$$
E(N)=
\int
L(\omega)
\max(N\cdot\omega,0)
d\omega
$$

其中：

* 正对法线的方向贡献最大
* 越靠近切线方向贡献越小
* 背面贡献为 0

---

## 12. 如何把 \(N\cdot L\) 预处理进 SH

先把环境光：

$$
L(\omega)
$$

投影成 SH 系数：

$$
c_l^m
$$

然后和 Lambert cosine kernel：

$$
\max(N\cdot\omega,0)
$$

做球面卷积。

在 SH 空间中，这个卷积会变成每阶系数的简单缩放：

$$
e_l^m=A_lc_l^m
$$

Lambert 常见系数：

$$
A_0=\pi
$$

$$
A_1=\frac{2\pi}{3}
$$

$$
A_2=\frac{\pi}{4}
$$

于是得到：

$$
e_l^m
$$

它表示的是：

> 已经考虑完整半球 \(N\cdot L\) 积分后的 irradiance 球面函数。

因此运行时只需要：

$$
E(N)
=
\sum_{l,m}
e_l^mY_l^m(N)
$$

就可以得到某个法线方向对应的环境漫反射。

关键理解：

> SH 系数不是存某一个法线的 \(N\cdot L\) 结果，而是存“所有可能法线对应结果”的整个球面函数。

---

## 13. Light Probe 空间插值

场景中会放多个 Probe。

Unity 会把 Probe 组织成四面体。

角色位于某个四面体内部时，根据角色位置得到四个插值权重：

$$
w_0+w_1+w_2+w_3=1
$$

对每个 SH 系数插值：

$$
c_i
=
w_0c_{i,0}
+w_1c_{i,1}
+w_2c_{i,2}
+w_3c_{i,3}
$$

得到角色当前位置的一整套 SH 系数。

然后再根据角色表面的法线：

$$
N
$$

计算：

$$
E(N)=\sum_i c_iY_i(N)
$$

因此可以记成：

$$
\boxed{\text{位置决定 SH 系数，法线决定如何读取 SH}}
$$

角色移动：

> Probe 插值得到的 SH 系数变化。

角色原地旋转：

> 法线变化，因此 \(Y_i(N)\) 变化。

---

# 最终核心总结

SH 可以理解成：

$$
\boxed{\text{球面函数的低频压缩表示}}
$$

基本过程：

$$
\text{球面光照}
\rightarrow
\text{投影到 SH 基函数}
\rightarrow
\text{得到 SH 系数}
$$

恢复：

$$
\text{方向}
\rightarrow
\text{计算 SH 基函数值}
\rightarrow
\text{加权 SH 系数}
\rightarrow
\text{得到颜色}
$$

Light Probe：

$$
\text{空间位置}
\rightarrow
\text{插值 Probe SH}
\rightarrow
\text{得到当前位置 SH}
$$

$$
\text{法线}
\rightarrow
\text{评估 SH}
\rightarrow
\text{得到环境漫反射}
$$

一句话记忆：

> **SH 用少量彼此正交的球面基函数，把复杂的方向光照压缩成少量系数；Light Probe 用 SH 存储空间中的低频环境漫反射光照。**
